<Application x:Class="OmniMarket.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:helpers="clr-namespace:OmniMarket.Helpers">
    <Application.Resources>
        <!-- ===== Converters ===== -->
        <helpers:ExpiryStatusConverter x:Key="ExpiryStatusColor"/>
        <helpers:ExpiryStatusTextConverter x:Key="ExpiryStatusText"/>
        <helpers:StockWarningConverter x:Key="StockWarningColor"/>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>

        <!-- String to Visibility (show if non-empty) -->
        <helpers:StringToVisibilityConverter x:Key="StringToVisibility"/>
        <!-- Zero count to Visibility (show if zero) -->
        <helpers:ZeroToVisibilityConverter x:Key="ZeroToVisibility"/>

        <!-- ===== Quantum-Neon Fusion Colors ===== -->
        <SolidColorBrush x:Key="CosmicDeepSpace" Color="#080414"/>
        <SolidColorBrush x:Key="NeonElectricBlue" Color="#00CED1"/>
        <SolidColorBrush x:Key="NeonMintGreen" Color="#2DD4BF"/>
        <SolidColorBrush x:Key="NeonVibrantGreen" Color="#39FF14"/>
        <SolidColorBrush x:Key="NeonAlarmRed" Color="#FF073A"/>
        <SolidColorBrush x:Key="NeonWarningAmber" Color="#f59e0b"/>
        <SolidColorBrush x:Key="NeonMoltenOrange" Color="#FF4500"/>
        <SolidColorBrush x:Key="NeonPurple" Color="#a855f7"/>
        
        <!-- Cosmic Nebula Gradient Background -->
        <LinearGradientBrush x:Key="SpaceBackground" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#080414" Offset="0.0"/>
            <GradientStop Color="#0f0728" Offset="0.5"/>
            <GradientStop Color="#0B132B" Offset="1.0"/>
        </LinearGradientBrush>

        <!-- ===== Shadow & Glow Effects ===== -->
        <DropShadowEffect x:Key="CardShadow" BlurRadius="20" Direction="270"
                          ShadowDepth="4" Opacity="0.3" Color="#000000"/>
        
        <DropShadowEffect x:Key="GlowBlue" BlurRadius="15" ShadowDepth="0" Color="#00CED1" Opacity="0.7"/>
        <DropShadowEffect x:Key="GlowRed" BlurRadius="20" ShadowDepth="0" Color="#FF073A" Opacity="0.8"/>
        <DropShadowEffect x:Key="GlowGreen" BlurRadius="15" ShadowDepth="0" Color="#39FF14" Opacity="0.6"/>
        <DropShadowEffect x:Key="GlowAmber" BlurRadius="15" ShadowDepth="0" Color="#f59e0b" Opacity="0.7"/>
        <DropShadowEffect x:Key="GlowOrange" BlurRadius="25" ShadowDepth="0" Color="#FF4500" Opacity="0.9"/>
        <DropShadowEffect x:Key="GlowPurple" BlurRadius="15" ShadowDepth="0" Color="#a855f7" Opacity="0.7"/>
        
        <!-- Glassmorphism Panel Style -->
        <Style x:Key="GlassmorphismPanel" TargetType="Border">
            <Setter Property="Background" Value="#15FFFFFF"/>
            <Setter Property="BorderBrush" Value="#30FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="16"/>
        </Style>

        <!-- ===== ModernTextBox ===== -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#10ffffff"/>
            <Setter Property="Foreground" Value="#e2e8f0"/>
            <Setter Property="CaretBrush" Value="#00CED1"/>
            <Setter Property="BorderBrush" Value="#30ffffff"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <Grid>
                                <TextBlock Text="{TemplateBinding Tag}" Foreground="#475569"
                                           VerticalAlignment="Center" FontSize="13"
                                           Visibility="{Binding Text.IsEmpty, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BoolToVisibility}}"/>
                                <ScrollViewer x:Name="PART_ContentHost" Margin="0"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#00CED1"/>
                                <Setter TargetName="Bd" Property="Effect" Value="{StaticResource GlowBlue}"/>
                                <Setter TargetName="Bd" Property="Background" Value="#20ffffff"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ===== ModernPasswordBox ===== -->
        <Style x:Key="ModernPasswordBox" TargetType="PasswordBox">
            <Setter Property="Background" Value="#10ffffff"/>
            <Setter Property="Foreground" Value="#e2e8f0"/>
            <Setter Property="CaretBrush" Value="#00CED1"/>
            <Setter Property="BorderBrush" Value="#30ffffff"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#00CED1"/>
                                <Setter TargetName="Bd" Property="Effect" Value="{StaticResource GlowBlue}"/>
                                <Setter TargetName="Bd" Property="Background" Value="#20ffffff"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ===== ModernComboBox ===== -->
        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="#0f172a"/>
            <Setter Property="Foreground" Value="#F8FAFC"/>
            <Setter Property="BorderBrush" Value="#334155"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Foreground" Value="#F8FAFC"/>
                        <Setter Property="Background" Value="#1e293b"/>
                        <Setter Property="Padding" Value="10,6"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#334155"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#3b82f6"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ===== ModernDatePicker ===== -->
        <Style x:Key="ModernDatePicker" TargetType="DatePicker">
            <Setter Property="Background" Value="#0f172a"/>
            <Setter Property="Foreground" Value="#e2e8f0"/>
            <Setter Property="BorderBrush" Value="#334155"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <!-- ===== Button Base ===== -->
        <Style x:Key="BaseButton" TargetType="Button">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Primary (Blue) -->
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource BaseButton}">
            <Setter Property="Background" Value="#3b82f6"/>
            <Setter Property="Foreground" Value="#ffffff"/>
        </Style>

        <!-- Success (Green) -->
        <Style x:Key="SuccessButton" TargetType="Button" BasedOn="{StaticResource BaseButton}">
            <Setter Property="Background" Value="#22c55e"/>
            <Setter Property="Foreground" Value="#ffffff"/>
        </Style>

        <!-- Warning (Amber) -->
        <Style x:Key="WarningButton" TargetType="Button" BasedOn="{StaticResource BaseButton}">
            <Setter Property="Background" Value="#f59e0b"/>
            <Setter Property="Foreground" Value="#ffffff"/>
        </Style>

        <!-- Danger (Red) -->
        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource BaseButton}">
            <Setter Property="Background" Value="#ef4444"/>
            <Setter Property="Foreground" Value="#ffffff"/>
        </Style>

        <!-- Secondary (Gray outline) -->
        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource BaseButton}">
            <Setter Property="Background" Value="#334155"/>
            <Setter Property="Foreground" Value="#e2e8f0"/>
        </Style>

        <!-- Small Button -->
        <Style x:Key="SmallButton" TargetType="Button" BasedOn="{StaticResource BaseButton}">
            <Setter Property="Background" Value="#334155"/>
            <Setter Property="Foreground" Value="#94a3b8"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Padding" Value="12,6"/>
        </Style>

        <!-- Sidebar Button (Neon Glow Hover) -->
        <Style x:Key="SidebarButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#94a3b8"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <!-- Neon parlayan arkaplan (Hover için) -->
                            <Border x:Name="GlowBg" Background="#00CED1" Opacity="0" CornerRadius="8" Margin="0,2"/>
                            
                            <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                    CornerRadius="8" Padding="{TemplateBinding Padding}" Margin="0,2">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <!-- Neon Çizgi İndikatör -->
                                    <Border x:Name="NeonIndicator" Grid.Column="0" Width="4" Height="20" 
                                            Background="#00CED1" CornerRadius="2" Margin="-8,0,12,0" 
                                            Opacity="0" Effect="{StaticResource GlowBlue}"/>
                                    
                                    <ContentPresenter Grid.Column="1" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                      VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#15ffffff"/>
                                <Setter Property="Foreground" Value="#00CED1"/>
                                <!-- Neon Indicator animasyonla (opacity) belirtebiliriz ama basit trigger da iş görür -->
                                <Setter TargetName="NeonIndicator" Property="Opacity" Value="1"/>
                                <Setter TargetName="GlowBg" Property="Opacity" Value="0.1"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#25ffffff"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Sidebar Button for Logs (Neon Purple Glow Hover) -->
        <Style x:Key="SidebarLogButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#94a3b8"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <!-- Neon parlayan arkaplan (Hover için) -->
                            <Border x:Name="GlowBg" Background="#a855f7" Opacity="0" CornerRadius="8" Margin="0,2"/>
                            
                            <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                    CornerRadius="8" Padding="{TemplateBinding Padding}" Margin="0,2">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <!-- Neon Çizgi İndikatör -->
                                    <Border x:Name="NeonIndicator" Grid.Column="0" Width="4" Height="20" 
                                            Background="#a855f7" CornerRadius="2" Margin="-8,0,12,0" 
                                            Opacity="0" Effect="{StaticResource GlowPurple}"/>
                                    
                                    <ContentPresenter Grid.Column="1" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                      VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#15ffffff"/>
                                <Setter Property="Foreground" Value="#a855f7"/>
                                <Setter TargetName="NeonIndicator" Property="Opacity" Value="1"/>
                                <Setter TargetName="GlowBg" Property="Opacity" Value="0.1"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#25ffffff"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Test / Demo Button (Molten Lava Glow) -->
        <Style x:Key="TestButton" TargetType="Button">
            <Setter Property="Background" Value="#FF4500"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                BorderThickness="0" CornerRadius="12" Padding="{TemplateBinding Padding}"
                                Effect="{StaticResource GlowOrange}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#ff5714"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#e03e00"/>
                                <Setter TargetName="Bd" Property="Effect" Value="{x:Null}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>
