<UserControl x:Class="OmniMarket.Views.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:OmniMarket.ViewModels">
    <UserControl.DataContext>
        <vm:RegisterViewModel/>
    </UserControl.DataContext>

    <UserControl.Resources>
        <!-- Floating Animations for cute mascot and background -->
        <Storyboard x:Key="FloatingAnim" RepeatBehavior="Forever" AutoReverse="True">
            <DoubleAnimation Storyboard.TargetName="Mascot" 
                             Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                             From="-15" To="10" Duration="0:0:2.5">
                <DoubleAnimation.EasingFunction>
                    <SineEase EasingMode="EaseInOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="BgBlob1" 
                             Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                             From="-30" To="30" Duration="0:0:8">
                <DoubleAnimation.EasingFunction>
                    <SineEase EasingMode="EaseInOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="BgBlob2" 
                             Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                             From="-40" To="40" Duration="0:0:11">
                <DoubleAnimation.EasingFunction>
                    <SineEase EasingMode="EaseInOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </UserControl.Resources>

    <UserControl.Triggers>
        <EventTrigger RoutedEvent="FrameworkElement.Loaded">
            <BeginStoryboard Storyboard="{StaticResource FloatingAnim}"/>
        </EventTrigger>
    </UserControl.Triggers>

    <Grid>
        <!-- Cute Vibrant Background Gradient -->
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#FFEDFA" Offset="0"/>
                <GradientStop Color="#FFF2E8" Offset="0.5"/>
                <GradientStop Color="#EAF2FF" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <!-- Blobs for "Süslü" look -->
        <Grid IsHitTestVisible="False" ClipToBounds="True">
            <Ellipse x:Name="BgBlob1" Width="500" Height="500" Fill="#FFD1E8" Opacity="0.5" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="-150,-150,0,0">
                <Ellipse.Effect><BlurEffect Radius="80"/></Ellipse.Effect>
                <Ellipse.RenderTransform><TranslateTransform/></Ellipse.RenderTransform>
            </Ellipse>
            <Ellipse x:Name="BgBlob2" Width="600" Height="600" Fill="#D1E8FF" Opacity="0.5" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,-200,-200">
                <Ellipse.Effect><BlurEffect Radius="100"/></Ellipse.Effect>
                <Ellipse.RenderTransform><TranslateTransform/></Ellipse.RenderTransform>
            </Ellipse>
            <Ellipse Width="350" Height="350" Fill="#FFF3B0" Opacity="0.4" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="300,-350,0,0">
                <Ellipse.Effect><BlurEffect Radius="60"/></Ellipse.Effect>
            </Ellipse>
        </Grid>

        <!-- Cute Sparkles Emoji -->
        <Grid IsHitTestVisible="False" ClipToBounds="True">
            <TextBlock Text="✨" FontSize="50" Opacity="0.8" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="100,100,0,0"/>
            <TextBlock Text="🎀" FontSize="65" Opacity="0.4" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,80,100,0" RenderTransformOrigin="0.5,0.5">
                <TextBlock.RenderTransform>
                    <RotateTransform Angle="15"/>
                </TextBlock.RenderTransform>
            </TextBlock>
            <TextBlock Text="🧁" FontSize="70" Opacity="0.3" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="150,0,0,100"/>
            <TextBlock Text="💖" FontSize="45" Opacity="0.6" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,150,150"/>
        </Grid>

        <!-- Main Card Container -->
        <Grid VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,50,0,0">
            
            <!-- Glass Card -->
            <Border Width="450" Background="#E6FFFFFF" CornerRadius="40" Padding="45,65,45,35"
                    BorderBrush="#FFFFFF" BorderThickness="4">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="50" Color="#F472B6" ShadowDepth="15" Opacity="0.25"/>
                </Border.Effect>

                <StackPanel>
                    <!-- Geri Dön Butonu -->
                    <Button Content="← Geri Dön" Command="{Binding GoToLoginCommand}"
                            Style="{StaticResource BaseButton}" Background="Transparent"
                            HorizontalAlignment="Left" Foreground="#9CA3AF"
                            Padding="0" Margin="0,-20,0,10" FontSize="13" FontWeight="SemiBold"/>

                    <TextBlock Text="Aramıza Katıl 🌸" FontSize="32" FontWeight="Heavy" 
                               FontFamily="Comic Sans MS, Segoe UI Print, Segoe UI" 
                               Foreground="#EC4899" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                    <TextBlock Text="Yeni Bir Kasaba Macerası!" FontSize="15" 
                               Foreground="#9CA3AF" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,0,0,30"/>

                    <!-- Market Adı -->
                    <TextBlock Text="Market Adı 🏪" Foreground="#F472B6" FontSize="13" FontWeight="Bold" Margin="0,0,0,6"/>
                    <TextBox Text="{Binding MarketName, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource ModernTextBox}" 
                             Foreground="#BE185D" Background="#FFF1F2" BorderBrush="#FBCFE8"
                             Tag="🦄 Marketinizin adını girin" Margin="0,0,0,16" Padding="15,12" FontSize="14"/>

                    <!-- Kullanıcı Adı -->
                    <TextBlock Text="Kullanıcı Adı 🍭" Foreground="#F472B6" FontSize="13" FontWeight="Bold" Margin="0,0,0,6"/>
                    <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource ModernTextBox}" 
                             Foreground="#BE185D" Background="#FFF1F2" BorderBrush="#FBCFE8"
                             Tag="✨ Mistik bir kullanıcı adı girin" Margin="0,0,0,16" Padding="15,12" FontSize="14"/>

                    <!-- Şifre -->
                    <TextBlock Text="Şifre 🔑" Foreground="#F472B6" FontSize="13" FontWeight="Bold" Margin="0,0,0,6"/>
                    <PasswordBox x:Name="PasswordBox"
                                 Style="{StaticResource ModernPasswordBox}" 
                                 Foreground="#BE185D" Background="#FFF1F2" BorderBrush="#FBCFE8"
                                 Margin="0,0,0,12" Padding="15,12" FontSize="14"/>

                    <!-- Hata Mesajı -->
                    <Border Background="#FEE2E2" CornerRadius="12" Padding="12" Margin="0,0,0,10"
                            Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibility}}">
                        <TextBlock Text="{Binding ErrorMessage}" Foreground="#EF4444"
                                   FontSize="13" FontWeight="SemiBold" HorizontalAlignment="Center" TextWrapping="Wrap"/>
                    </Border>
                    
                    <!-- Başarı Mesajı -->
                    <Border Background="#D1FAE5" CornerRadius="12" Padding="12" Margin="0,0,0,10"
                            Visibility="{Binding SuccessMessage, Converter={StaticResource StringToVisibility}}">
                        <TextBlock Text="{Binding SuccessMessage}" Foreground="#10B981"
                                   FontSize="13" FontWeight="SemiBold" HorizontalAlignment="Center" TextWrapping="Wrap"/>
                    </Border>

                    <!-- Kayıt Butonu -->
                    <Button Content="✨ Puanları Topla (Kayıt Ol) ✨" Command="{Binding RegisterCommand}"
                            Style="{StaticResource CuteButton}" Margin="0,10,0,16">
                        <Button.Effect>
                            <DropShadowEffect BlurRadius="20" Color="#F472B6" ShadowDepth="6" Opacity="0.5"/>
                        </Button.Effect>
                    </Button>

                    <!-- Giriş Linki -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
                        <TextBlock Text="Zaten maceranın içinde misin? " Foreground="#9CA3AF" FontSize="13" FontWeight="Medium"/>
                        <TextBlock FontSize="13" FontWeight="Bold">
                            <Hyperlink Command="{Binding GoToLoginCommand}"
                                       Foreground="#EC4899" TextDecorations="Underline">
                                Giriş Yap 🎀
                            </Hyperlink>
                        </TextBlock>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Floating Mascot Overlapping Top -->
            <Border x:Name="Mascot" Width="120" Height="120" Background="#FFFFFF" CornerRadius="60" 
                    VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,-60,0,0"
                    BorderBrush="#FDE047" BorderThickness="4">
                <Border.RenderTransform>
                    <TranslateTransform X="0" Y="0"/>
                </Border.RenderTransform>
                <Border.Effect>
                    <DropShadowEffect BlurRadius="25" Color="#FDE047" ShadowDepth="10" Opacity="0.7"/>
                </Border.Effect>
                <TextBlock Text="🏪" FontSize="80" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,10"/>
            </Border>

        </Grid>
    </Grid>
</UserControl>
